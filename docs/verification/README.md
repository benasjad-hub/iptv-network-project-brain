# Verification evidence

**Empty.** No verification record has been filed for this project yet.

One directory per verification, named `<TOPIC>_<YYYY-MM-DD>/`, each holding a
`README.md` that names **the commit tested**, **the date**, and **the limits of
what it proves**. The structural check enforces all three, because evidence that
claims no boundary will be read as proving everything.

A record is a claim about a commit. One without a commit proves nothing about any
tree.
